# Database connection — when running outside Docker, use localhost
# Inside Docker the compose file overrides this with the service name (db)
DATABASE_URL=postgresql://admin:adminpassword@localhost:5432/nicattobeard_db

# JWT secret for authentication tokens
JWT_SECRET=dev-secret-change-in-production

# Server port
PORT=3001
